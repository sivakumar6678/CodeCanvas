import PageLoader from '../../components/ui/PageLoader';

export default function Loading() {
  return <PageLoader title="Loading studio" message="Preparing private admin data and analytics..." />;
}
