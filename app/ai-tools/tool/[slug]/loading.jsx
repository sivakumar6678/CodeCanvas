import PageLoader from '../../../../components/ui/PageLoader';

export default function Loading() {
  return <PageLoader title="Loading tool details" message="Preparing ratings, comments, and related recommendations..." />;
}
