import PageLoader from '../../components/ui/PageLoader';

export default function Loading() {
  return <PageLoader title="Loading AI tools" message="Fetching categories, featured tools, and search results..." />;
}
