import PageLoader from '../../components/ui/PageLoader';

export default function Loading() {
  return <PageLoader title="Loading tools" message="Preparing built-in and external developer tools..." />;
}
